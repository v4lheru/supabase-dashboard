# Supabase Configuration
# Get these from your Supabase project settings: https://app.supabase.com/project/YOUR_PROJECT/settings/api

# Required: Your Supabase project URL
NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co

# Required: Your Supabase anon/public key (safe to expose in frontend)
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Optional: Service role key for server-side operations (keep secret)
# Only needed if you plan to use server-side admin operations
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Railway Deployment Notes:
# 1. Copy this file to .env.local for local development
# 2. In Railway, set these as environment variables in your service settings
# 3. Never commit .env.local to git (it's already in .gitignore)
